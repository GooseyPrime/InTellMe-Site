{
  "name": "intellme-site",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "InTellMe parent company site.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node -e \"console.log('investor request archived; no live tests')\"",
    "verify": "npm run check && npm run test && npm run build"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^7.2.10",
    "typescript": "^5.9.2"
  }
}
